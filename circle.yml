machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t flask-docker-workflow .

test:
  override:
    - docker run -p 8000:8000 flask-docker-workflow; sleep 10
    - curl --retry 10 --retry-delay 5 -v http://localhost:8000
    - python tests.py